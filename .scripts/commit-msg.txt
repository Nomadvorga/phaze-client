sky customizer dome tint, shulker preview vanilla chest style, tick-cached inventory hud rendering

- sky customizer: switch ClientWorld.getSkyColor hook to @ModifyReturnValue with priority 1300 over BadOpt's 1200, no more hand-port of vanilla math (fixes black sky on world join, no relog needed)
- sky customizer: new WorldRendererSkyDomeTintMixin redirects setShaderColor inside SkyRendering.renderSky so the actual sky-dome geometry picks up the tint regardless of upstream mixin stack (fixes "fog tinted but dome black" symptom)
- shulker preview: render with vanilla generic_54.png chest texture in three strips (top border 7px, slot rows 54px, bottom border 7px) so top and bottom frames match instead of carrying the wide vanilla title bar
- shulker preview: new "Color By Shulker" toggle that tints the chest texture with the shulker's dye colour using vanilla's 0.65 lift toward white
- shulker preview: per-tick cache for the resolved container contents and overlay flags, killing the per-frame streamNonEmpty walk and cooldown lookups
- inventory hud: per-tick snapshot of the 27 main slots + precomputed overlay flags, render path is now a flat array read with zero allocations
- freelook: F3+key combos now skip activation (vanilla debug toggles work), F5 deactivates freelook so player can rotate normally
- mouse zoom: redirect changeLookDirection inside Mouse.updateMouse so smooth + cinematic camera processing reaches FreeLook
